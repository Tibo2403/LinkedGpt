import React from 'react';

/**
 * Application header displaying the site title.
 */

/**
 * Renders the main application heading.
 */
const Header: React.FC = () => (
  <h1 className="text-3xl font-bold text-blue-600">LinkedGPT</h1>
);

export default Header;
